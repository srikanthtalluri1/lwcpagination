handleRefreshPath() {
    // delay in milliseconds (example: 2000 = 2 seconds)
    setTimeout(() => {
        this.dispatchEvent(
            new CustomEvent('valuechange', {
                detail: 'Hello from Child A'
            })
        );
    }, 2000);
}
