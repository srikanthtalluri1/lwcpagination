const manualSlug = row.paamManual?.trim()
                        ? statusSlug(row.manualStatus || 'Unknown')
                        : 'empty';

                    const discoveredSlug = row.paamDiscovered?.trim()
                        ? statusSlug(
                            row.discoveredStatus ||
                            row.manualStatus ||
                            'Unknown'
                        )
                        : 'empty';
