   @AuraEnabled(cacheable=true)
    public static Integer getUniqueFirstNameCount() {

        Set<String> uniqueNames = new Set<String>();

        for (Contact con : [SELECT FirstName FROM Contact WHERE FirstName != NULL]) {
            uniqueNames.add(con.FirstName);
        }

        return uniqueNames.size();
    }
