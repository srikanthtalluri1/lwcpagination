 fireShowfilterEvent(){
         const evt = new CustomEvent('filtercheck', 
                    {detail:{showFilter : false} });
         this.dispatchEvent(evt);
         this.hasPadding = !this.hasPadding;
          // Toggle a class on the host element itself
        this.template.host.classList.toggle('no-padding', !this.hasPadding);
    }
