 if(key=='Requesting Domain'){
                              fieldApiName='TechnologyDecision_Relationship__r.Requesting_Domain__r.Name';
                          }else{
                              List<String> apiNames = TechnologyDecision_DataTableHandler.getApiNamesFromLabels('TechnologyDecision__c', key);
                              fieldApiName =apiNames[0]; 
                          }

'Requesting Domain' => 'requestingDomain'

 if(fieldApiNameString.contains('Requesting Domain')){
         query = 'SELECT TechnologyDecision_Relationship__r.Requesting_Domain__r.Name requestingDomain, COUNT(Id) cnt ' +
        'FROM TechnologyDecision__c ' + condition +
        ' GROUP BY TechnologyDecision_Relationship__r.Requesting_Domain__r.Name';

			results.put('Requesting Domain', Database.query(query));
        } 

 String valueKey = field == 'Requesting Domain' ? 'requestingDomain' : fieldList.get(field);
                  String countKey = field == 'Requesting Domain' ? 'cnt' : 'expr0';
                    
                    uni.put('value', (String) ar.get(valueKey));
                    uni.put('count', (Integer) ar.get(countKey));

 { label: 'Requesting Domain', value: 'Requesting Domain', selected: false, class: 'slds-form-element__label' }

