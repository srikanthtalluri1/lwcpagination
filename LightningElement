onclick={handleClickDate}

 handleClickDate(event){
            const pDate = event.detail.value
            if(pDate == null){
                this.isEditPlannedDateofResolution = true;
            }
    }

if(field.value !='' && this.isEditPlannedDateofResolution == true){
