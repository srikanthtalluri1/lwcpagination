 get filteredFieldValues() {
    return (this._filteredFieldValues || []).map(item => ({
        ...item,
        field:
            item.filterField === 'Status'
                ? 'Status/Stage'
                : item.filterField === 'Requesting App Portfolio'
                ? 'Requesting App Portfolio/Domain'
                : item.filterField
    }));
}

