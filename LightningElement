AWP_Confluence_Published_Standard__c rec =
    new AWP_Confluence_Published_Standard__c();

rec.Zephyr_Standard_ID__c = wrapRec.Id;

// other field mappings

Database.upsert(
    rec,
    AWP_Confluence_Published_Standard__c.Fields.Zephyr_Standard_ID__c,
    AccessLevel.USER_MODE
);
