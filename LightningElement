handleDo() {
        const child = this.template.querySelector('c-child-component');
        if (child) {
            child.doSomething();
        }
    }

    handleRevoke() {
        const child = this.template.querySelector('c-child-component');
        if (child) {
            child.revokeSomething();
        }
    }
