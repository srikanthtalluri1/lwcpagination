if (tab == 'Application' && allOrMy == 'allwaivers') {
    String condition = ' Waiver_Id_c.Type_c = \'Application\' ';
    String prefix = !query.contains('WHERE') ? ' WHERE ' : ' AND ';
    query += prefix + condition;
    
    prefix = !countQuery.contains('WHERE') ? ' WHERE ' : ' AND ';
    countQuery += prefix + condition;
}
