  totalConflictCount = 0;
    totalSyncCount = 0;

  get totalConflicts() {
        return this.totalConflictCount;
    }

    get totalSync() {
        return this.totalSyncCount;
    }

    get conflictPct() {
        if (!this.totalRecordCount) {
            return 0;
        }

        return Math.round(
            (
                this.totalConflictCount /
                this.totalRecordCount
            ) * 100
        );
    }


 resetServerPagination() {
    this.page = 1;
    this.rows = [];
    this.lastAppName = null;
    this.lastRecordId = null;
    this.hasMore = true;
    this.totalRecordCount = 0;
    this.totalConflictCount = 0;
    this.totalSyncCount = 0;
}

 this.selectedRow = {
    ...row,

    appNameDisplay: row.appName || 'Not Available',
    operationalStatusDisplay: row.status || 'Not Available',

    portfolioDisplay: row.portfolio || 'Not Available',
    domainDisplay: row.domain || 'Not Available',
    categoryDisplay: row.category || 'Not Available',
    uuidDisplay: row.UIID || 'Not Available',
    appTypeDisplay: row.appType || 'Not Available',
    subtypeDisplay: row.subtype || 'Not Available',
    technicalArchitectDisplay: row.technicalArchitect || 'Not Available',
    businessOwnerDisplay: row.businessOwner || 'Not Available',
    applicationOwnerDisplay: row.ApplicationOwner || 'Not Available',
    descriptionDisplay: row.description || 'Not Available',
    timeDisplay: row.appTIME || 'Not Available',

    paamManualDisplay: row.paamManual || 'Not Available',
    paamDiscoveredDisplay: row.paamDiscovered || 'Not Available',

    stackLayerStatusDisplay:
        row.manualStatus ||
        row.discoveredStatus ||
        'Not Available'
};


