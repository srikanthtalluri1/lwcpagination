getUniqueAppCount() {
    getUniqueAppCount({
        ownedBy: this.ownedBy
    })
    .then(result => {
        this.uniqueAppCount = result;
    })
    .catch(error => {
        console.error('Error getting unique app count:', error);
    });
}
