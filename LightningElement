let errorArray='';
       if(!this.waiverData['Name']){
        console.log('checking here');
        errorArray = 'Name' ;
        this.isError=true;
       }
       if(!this.waiverData['Renew_Counter__c']){
        errorArray = errorArray+' , '+'Renew Counter';
         this.isError=true;
       }
       
        console.log('checking ',this.waiverData['Name']);
        console.log('checking errorArray ',errorArray);
        if(this.isError){
           this.showToast('Error', `${errorArray} fields are required` , 'error');  
        }
        if(this.isError==false){
