pubsub.subscribe('saveSuccess', this.handleEditDisableButton.bind(this));  55-62
        console.log('Subscribed to saveSuccess event');
    }

    handleEditDisableButton(event) {
        console.log('event fired 60'+JSON.stringify(event));
        this.disableButton = event;
    }


onclick={handleActions} disabled={disableButton}></lightning-button> 22 edit button

 disableButton = false;- 153

 pubsub.publish('editClick', false);  169-172

                console.log('this.disableButton 161 '+this.disableButton);
                this.disableButton = true;

 pubsub.publish('editClick', true);- 182
