 stackStatus: this.selectedStackStatus

 get selectedStackStatus() {
    if (this.statusFilter === 'all') {
        return null;
    }

    const selectedOption = this.stackStatusOptions.find(
        (option) => option.slug === this.statusFilter
    );

    return selectedOption
        ? selectedOption.label
        : null;
}


 this.loadRecords();
