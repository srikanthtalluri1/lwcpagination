({
    doInit : function(component, event, helper) {
        // Record ID is automatically populated by force:hasRecordId
        let recId = component.get("v.recordId");
        console.log("Record ID from Aura Override: " + recId);

        // Stop spinner
        component.set("v.isLoading", false);
    }
})
