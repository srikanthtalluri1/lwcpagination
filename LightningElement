if (tab == 'Application' && allOrMy == 'allwaivers') {
    String queryCondition = "Waiver_Id__r.Type__c = 'Application'";
    
    query += query.contains('WHERE') ? ' AND ' + queryCondition : ' WHERE ' + queryCondition;
    countQuery += countQuery.contains('WHERE') ? ' AND ' + queryCondition : ' WHERE ' + queryCondition;
}
